# pnpm settings. This pnpm reads the install-script allowlist from here, not package.json.
# Supply-chain hygiene (CLAUDE.md): only these packages may run install scripts. esbuild is
# vitest's transform engine — a devDependency; nothing here ships in the SDK (ADR-0014).
allowBuilds:
  esbuild: true
